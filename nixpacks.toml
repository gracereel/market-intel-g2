[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd web-app && npm install"]

[phases.build]
cmds = ["cd web-app && npm run build"]

[start]
cmd = "cd web-app && node dist/index.cjs"
